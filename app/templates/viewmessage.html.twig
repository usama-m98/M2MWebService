{% extends 'banner.html.twig' %}
{% block content %}
    <h2>{{ page_heading_2 }}</h2>
    <p>Sent Messages</p>
    <form action="{{ action }}" method="{{ method }}">
     {% for position, info in data %}
         <p>{{ info }}</p>
         <br>
    {% endfor %}
    <input type="submit" value="download messages">
    </form>
{% endblock %}